name: integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  integration:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: trade_infra
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v3
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Install libpq-dev
        run: sudo apt-get install -y libpq-dev

      - name: Build C++ libraries
        run: |
          cmake -B market-data-svc/build -S market-data-svc \
            -DRust_COMPILER="$(rustup which rustc)" -DBUILD_TESTS=OFF
          cmake --build market-data-svc/build -j4
          cmake -B risk-svc/build -S risk-svc \
            -DRust_COMPILER="$(rustup which rustc)" -DBUILD_TESTS=OFF
          cmake --build risk-svc/build -j4

      - name: Build Go services
        run: |
          cd order-svc && go build ./cmd/server
          cd ../risk-svc && go build ./cmd/server

      - name: Start stack
        working-directory: trade_infra/infra
        run: docker compose up -d --wait
        timeout-minutes: 5

      - name: Install smoke test deps
        run: cd python && uv sync --extra dev

      - name: Run smoke test
        run: cd python && uv run python smoke_test.py

      - name: Dump logs on failure
        if: failure()
        working-directory: trade_infra/infra
        run: docker compose logs

      - name: Tear down
        if: always()
        working-directory: trade_infra/infra
        run: docker compose down -v
